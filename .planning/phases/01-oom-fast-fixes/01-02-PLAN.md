---
phase: 01-oom-fast-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 01-oom-fast-fixes-01
files_modified:
  - src/kafka/subscribers/redis-subscriber.ts
  - src/kafka/subscribers/messages.ts
  - src/kafka/subscribers/constants.ts
autonomous: true

must_haves:
  truths:
    - "In multi-instance (Redis) mode, per-subscriber message lists are bounded (cannot grow unbounded within TTL)."
    - "No code path in Redis message retrieval reads the full list (`LRANGE 0 -1`) in request/debug paths."
  artifacts:
    - path: src/kafka/subscribers/redis-subscriber.ts
      provides: "Bounded Redis list append via RPUSH + LTRIM (+ EXPIRE)"
    - path: src/kafka/subscribers/messages.ts
      provides: "Bounded tail reads from Redis"
  key_links:
    - from: src/kafka/subscribers/redis-subscriber.ts
      to: Redis
      via: "RPUSH + LTRIM + EXPIRE"
      pattern: "LTRIM"
    - from: src/kafka/subscribers/messages.ts
      to: Redis
      via: "LRANGE tail reads"
      pattern: "LRANGE.*-"
---

<objective>
Bound Redis-backed message retention and prevent unbounded reads that can inflate Node heap.

Purpose: Stop both Redis memory growth and Node heap spikes caused by pulling/serializing huge arrays.
Output: Redis message lists trimmed on write + bounded tail reads.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.plannning/OOM-ROOT-CAUSE-AND-FAST-FIXES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim Redis lists on write (RPUSH + LTRIM + EXPIRE)</name>
  <files>
src/kafka/subscribers/redis-subscriber.ts
src/kafka/subscribers/constants.ts
  </files>
  <action>
- In `RedisSubscriber.addMessage()` (or equivalent message append method):
  - keep current `RPUSH` behavior
  - add `LTRIM key -MAX_LIST_LENGTH -1` immediately after push
  - keep/ensure `EXPIRE key TTL` remains in place

- Define `MAX_LIST_LENGTH` using the same cap as single-instance (`MAX_SUBSCRIBER_MESSAGES`) to keep behavior consistent.

Constraints:
- Use Redis multi/transaction/pipeline if already present (follow existing client style).
- Ensure trimming applies to the *actual* message list key used for storage.
  </action>
  <verify>
yarn build
  </verify>
  <done>
- Redis message list cannot exceed MAX_SUBSCRIBER_MESSAGES elements regardless of traffic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace unbounded Redis list reads with tail reads</name>
  <files>
src/kafka/subscribers/messages.ts
  </files>
  <action>
- Find Redis message retrieval helper(s) (notably `getMessagesFromRedis`).
- Replace any `LRANGE 0 -1` usage with a bounded tail read:
  - `LRANGE key -N -1` where N = `MAX_SUBSCRIBER_MESSAGES` (or smaller default window if already configured)

This must cover request path usage (e.g., `/consume`) and `/debug` usage.

Constraints:
- Do not introduce new behavior that requires a cursor protocol in this plan (fast fix only).
- Keep return types the same; just bound the amount of data loaded.
  </action>
  <verify>
yarn build
  </verify>
  <done>
- No `LRANGE 0 -1` remains in Redis message retrieval used by runtime endpoints.
- Memory usage no longer scales linearly with time within TTL due to bounded lists.
  </done>
</task>

</tasks>

<verification>
- `yarn build` passes.
</verification>

<success_criteria>
- Redis mode message storage is bounded by list trimming.
- Runtime request/debug paths do not read unbounded Redis lists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oom-fast-fixes/01-02-SUMMARY.md`
</output>
