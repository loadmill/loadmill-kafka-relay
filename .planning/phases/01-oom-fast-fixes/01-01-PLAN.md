---
phase: 01-oom-fast-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kafka/subscribers/constants.ts
  - src/kafka/subscribers/subscriber.ts
  - src/kafka/subscribers/subscribers-manager.ts
  - src/kafka/subscribers/message-limits.ts
  - test/kafka/message-limits.test.ts
autonomous: true

must_haves:
  truths:
    - "In single-instance mode, subscriber message retention is bounded (cannot grow unbounded until OOM)."
    - "When caps are exceeded, oldest messages are evicted (drop-oldest policy)."
  artifacts:
    - path: src/kafka/subscribers/message-limits.ts
      provides: "Centralized message limit logic (max messages + max bytes)"
    - path: src/kafka/subscribers/subscriber.ts
      provides: "Applies message limits during addMessage()"
    - path: test/kafka/message-limits.test.ts
      provides: "Regression tests for limit/eviction behavior"
  key_links:
    - from: src/kafka/subscribers/subscriber.ts
      to: src/kafka/subscribers/message-limits.ts
      via: "enforceMessageLimits(...)"
      pattern: "enforceMessageLimits\("
    - from: src/kafka/subscribers/constants.ts
      to: src/kafka/subscribers/subscriber.ts
      via: "MAX_SUBSCRIBER_(MESSAGES|BYTES)"
      pattern: "MAX_SUBSCRIBER_(MESSAGES|BYTES)"
---

<objective>
Bound single-instance memory growth by enforcing hard caps on per-subscriber retained messages.

Purpose: Prevent heap growth proportional to traffic/subscriber count that leads to Node OOM.
Output: A bounded in-memory message store + tests proving eviction/caps.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.plannning/OOM-ROOT-CAUSE-AND-FAST-FIXES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded message-limit utilities + env-backed defaults</name>
  <files>
src/kafka/subscribers/message-limits.ts
src/kafka/subscribers/constants.ts
  </files>
  <action>
- Create `src/kafka/subscribers/message-limits.ts` containing small, testable helpers to:
  - estimate message byte size (cheap approximation is OK; do NOT stringify full objects)
  - enforce both `maxMessages` and `maxBytes` with a drop-oldest eviction loop
  - return eviction metadata (e.g., { droppedCount, droppedBytes }) for optional logging

- Extend `src/kafka/subscribers/constants.ts` to add env-configurable hard caps (with safe defaults):
  - `MAX_SUBSCRIBER_MESSAGES` (default: 1000; clamp to a sane range)
  - `MAX_SUBSCRIBER_BYTES` (default: 10 MiB; clamp to a sane range)

Constraints:
- Keep code allocation-light; avoid JSON.parse/stringify in limit enforcement.
- Follow existing env parsing patterns (this repo already parses env vars in several modules).
  </action>
  <verify>
yarn test
  </verify>
  <done>
- New module exists and is imported nowhere yet (ok).
- constants expose validated numeric caps with defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce caps in in-memory Subscriber message retention</name>
  <files>
src/kafka/subscribers/subscriber.ts
src/kafka/subscribers/subscribers-manager.ts
  </files>
  <action>
- Update `Subscriber.addMessage()` (single-instance path) so retained messages are always bounded:
  - append new message
  - apply `enforceMessageLimits(...)` using the configured caps
  - evict oldest messages until under both caps

- Ensure any in-memory accessors (`getMessages()` and any debug helpers) only ever return the already-bounded list.

Notes:
- Keep policy strictly drop-oldest (fast fix). Do NOT implement per-subscriber duplication changes here.
- If there is existing 10-minute cleanup, do not remove it; caps are the primary safety net.
  </action>
  <verify>
yarn test
  </verify>
  <done>
- In-memory subscriber message list cannot exceed configured message count or approximate bytes.
- System behavior under load degrades by dropping old messages, not by OOM.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add focused unit tests for eviction + cap behavior</name>
  <files>
test/kafka/message-limits.test.ts
  </files>
  <action>
- Add Jest unit tests covering `enforceMessageLimits(...)`:
  - evicts oldest when `maxMessages` exceeded
  - evicts oldest when `maxBytes` exceeded
  - handles combined constraints deterministically
  - preserves message order for retained messages

- Keep tests deterministic and fast (no Kafka/Redis/Fastify).
  </action>
  <verify>
yarn test
  </verify>
  <done>
- Tests pass and fail meaningfully if eviction logic regresses.
  </done>
</task>

</tasks>

<verification>
- `yarn test` passes.
- `yarn build` passes (lint + tsc + test).
</verification>

<success_criteria>
- Single-instance mode has hard caps on retained messages per subscriber (count + bytes).
- Eviction is drop-oldest and covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oom-fast-fixes/01-01-SUMMARY.md`
</output>
