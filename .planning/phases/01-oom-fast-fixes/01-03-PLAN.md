---
phase: 01-oom-fast-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router.ts
  - src/server-validation/index.ts
  - src/kafka/consume.ts
  - src/kafka/debug/index.ts
autonomous: true

must_haves:
  truths:
    - "`GET /consume/:id` always returns a bounded number of messages (default limit, enforced max)."
    - "`/debug` never returns an unbounded number of messages per subscriber."
  artifacts:
    - path: src/kafka/consume.ts
      provides: "Consume returns bounded results and avoids building huge arrays"
    - path: src/router.ts
      provides: "Parses/enforces `limit` query parameter"
    - path: src/kafka/debug/index.ts
      provides: "Debug output is bounded by default"
  key_links:
    - from: src/router.ts
      to: src/kafka/consume.ts
      via: "limit query -> consume options"
      pattern: "limit"
---

<objective>
Make API responses bounded so request-time JSON parse/stringify cannot OOM the Node process.

Purpose: Even with bounded retention, endpoints must never attempt to serialize huge arrays.
Output: `/consume` paging + `/debug` output caps.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.plannning/OOM-ROOT-CAUSE-AND-FAST-FIXES.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `limit` to consume validation + enforce default/max in router</name>
  <files>
src/server-validation/index.ts
src/router.ts
  </files>
  <action>
- Extend consume query validation schema to accept an optional numeric `limit`.
- Enforce:
  - default `limit = 100`
  - max `limit = 1000` (clamp or 400; choose one and be consistent)

- Ensure router passes the resolved limit into the consume use-case.

Constraints:
- Do not allow unbounded results even if client omits limit.
- Keep behavior consistent in both single and multi-instance modes.
  </action>
  <verify>
yarn test
  </verify>
  <done>
- `/consume/:id` has a stable, documented bounded response size.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce consume result bounds in consume use-case</name>
  <files>
src/kafka/consume.ts
  </files>
  <action>
- Update consume flow so it never returns more than `limit` messages after filtering.
- Ensure internal loops stop early once enough matches are found (avoid scanning/processing more than necessary).
- Keep current behavior for `text` option and JSON parsing, but ensure parsing is only performed on the messages that will be returned.

Constraints:
- Avoid constructing intermediate arrays larger than needed.
  </action>
  <verify>
yarn test
  </verify>
  <done>
- Returned `messages.length <= limit` for all consume requests.
- Consume does not parse/stringify more than needed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cap debug output (sample + counts)</name>
  <files>
src/kafka/debug/index.ts
  </files>
  <action>
- Change `/debug` response to be safe by default:
  - return message counts per subscriber/topic
  - include only a bounded sample of messages (e.g., last 10) per subscriber

- If `/debug` already truncates message values, keep that, but also cap array lengths.

Constraints:
- `/debug` must not fetch/serialize full message backlogs.
  </action>
  <verify>
yarn build
  </verify>
  <done>
- Debug endpoint response size is bounded regardless of subscriber count/backlog.
  </done>
</task>

</tasks>

<verification>
- `yarn test` passes.
- `yarn build` passes.
</verification>

<success_criteria>
- `/consume` and `/debug` are bounded by design and cannot create huge JSON responses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oom-fast-fixes/01-03-SUMMARY.md`
</output>
